//  

import Foundation

// MARK: - Builder

/*
do {
    let config = try ResourcePoolConfig.Builder(name: "dbconnectionpool")
        .set(maxTotal: 16)
        .set(maxIdle: 10)
        .set(minIdle: 12)
        .build()
} catch {
    print(error)
}
*/


